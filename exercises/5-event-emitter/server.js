class Server {
    constructor (client) {

        // 1. wyslij wiadomosc powitalna do klienta - Wpisz komende (help wyświetla listę komend)

        // 2. podlacz sie do zdarzenia 'command' emitowanego przez obiekt 'client'

    }

    help () {
        // this.emit('response', `Dostępne komedny:
        // add task
        // ls
        // delete :id`
        // );
    }
    // 3. dodaj kolejne metody do obslugi
}

// 4. wyeksportuj funkcje przyjmujaca obiekt klienta i tworzaca instancje obiektu Server

